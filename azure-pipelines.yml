# https://aka.ms/yaml

trigger:
- master
pr:
- master

pool:
  vmImage: 'windows-latest'

steps:
- powershell: Invoke-Pester -Script *.Tests.ps1 -OutputFormat NUnitXml -OutputFile 'TEST-HelperModule.xmlCorr '
  displayName: 'Run helper module tests'
  workingDirectory: '$(System.DefaultWorkingDirectory)/Modules/MathshammerHelper/Tests'

- task: PublishTestResults@2
  inputs:
    testResultsFormat: NUnit
    testResultsFiles: '**/TEST-*.xml' 

